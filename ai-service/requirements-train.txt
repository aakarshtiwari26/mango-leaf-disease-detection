# Extra dependencies needed only for local training (app/train.py), not for
# serving predictions in production. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-train.txt
tensorflow==2.18.0
keras==3.10.0
scikit-learn==1.5.2
matplotlib==3.9.2
seaborn==0.13.2
